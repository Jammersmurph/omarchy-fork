#!/bin/bash

# omarchy:summary=Refresh the ~/.config/brave-flags.conf file from the Jammarchy defaults.

CONFIG_FILE="$HOME/.config/brave-flags.conf"

# Refresh the Brave configuration
omarchy-refresh-config brave-flags.conf
